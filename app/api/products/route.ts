import { randomUUID } from "node:crypto";
import { getRuntimeEnv } from "../../../db/runtime-env";
import { requireAccess } from "../../../db/authz";
const T="tenant_demo",B="branch_centro";
const userId=(req:Request)=>`user_${btoa(req.headers.get("oai-authenticated-user-email")||"preview@pos360.local").replace(/[^a-z0-9]/gi,"").slice(0,20)}`;
export async function GET(req:Request){const d=getRuntimeEnv().DB,url=new URL(req.url),q=`%${(url.searchParams.get("q")||"").trim()}%`;const rows=await d.prepare("SELECT id,sku,barcode,name,category,price,cost,stock,version,active,updated_at updatedAt FROM products WHERE tenant_id=? AND (name LIKE ? OR sku LIKE ? OR barcode LIKE ?) ORDER BY active DESC,name LIMIT 200").bind(T,q,q,q).all();return Response.json({products:rows.results});}
export async function POST(req:Request){const access=await requireAccess(req,"inventory");if(access.error)return access.error;const p=await req.json() as {sku?:string;barcode?:string;name?:string;category?:string;price?:number;cost?:number;stock?:number};if(!p.sku?.trim()||!p.name?.trim()||!p.category?.trim()||Number(p.price)<0||Number(p.cost)<0)return Response.json({error:"Complete los campos obligatorios"},{status:400});const d=getRuntimeEnv().DB,id=randomUUID(),stock=Number(p.stock||0),uid=userId(req);try{await d.batch([d.prepare("INSERT INTO products (id,tenant_id,sku,barcode,name,category,price,cost,stock) VALUES (?,?,?,?,?,?,?,?,?)").bind(id,T,p.sku.trim(),p.barcode?.trim()||null,p.name.trim(),p.category.trim(),Number(p.price),Number(p.cost),stock),d.prepare("INSERT INTO inventory_movements (id,tenant_id,branch_id,product_id,user_id,movement_type,quantity,balance_after,reason,reference) VALUES (?,?,?,?,?,?,?,?,?,?)").bind(randomUUID(),T,B,id,uid,"initial",stock,stock,"Inventario inicial",p.sku.trim())]);return Response.json({product:{id,...p,stock,active:1,version:1}},{status:201});}catch(e){return Response.json({error:e instanceof Error&&e.message.includes("UNIQUE")?"El SKU ya existe":"No fue posible crear el producto"},{status:409});}}
export async function PATCH(req:Request){const access=await requireAccess(req,"inventory");if(access.error)return access.error;const p=await req.json() as {id?:string;sku?:string;barcode?:string;name?:string;category?:string;price?:number;cost?:number;active?:boolean;adjustment?:number;reason?:string;version?:number;deviceId?:string};if(!p.id)return Response.json({error:"Producto requerido"},{status:400});const d=getRuntimeEnv().DB,current=await d.prepare("SELECT * FROM products WHERE id=? AND tenant_id=?").bind(p.id,T).first<{stock:number;version:number}>();if(!current)return Response.json({error:"Producto no encontrado"},{status:404});if(p.version!==undefined&&p.version!==current.version){const conflictId=randomUUID();await d.prepare("INSERT INTO sync_conflicts (id,tenant_id,entity_type,entity_id,device_id,local_version,server_version,payload,status) VALUES (?,?,?,?,?,?,?,?,?)").bind(conflictId,T,"product",p.id,p.deviceId||"unknown",p.version,current.version,JSON.stringify(p),"pending").run();return Response.json({error:"El producto cambió en otra sede",conflict:true,conflictId,serverVersion:current.version},{status:409});}const adjustment=Number(p.adjustment||0),nextStock=current.stock+adjustment;if(nextStock<0)return Response.json({error:"El ajuste dejaría existencias negativas"},{status:400});const sets:string[]=[],values:unknown[]=[];for(const [key,col] of [["sku","sku"],["barcode","barcode"],["name","name"],["category","category"],["price","price"],["cost","cost"]] as const){if(p[key]!==undefined){sets.push(`${col}=?`);values.push(p[key]);}}if(p.active!==undefined){sets.push("active=?");values.push(p.active?1:0)}if(adjustment){sets.push("stock=?");values.push(nextStock)}sets.push("version=version+1","updated_at=CURRENT_TIMESTAMP");values.push(p.id,T);const statements=[d.prepare(`UPDATE products SET ${sets.join(",")} WHERE id=? AND tenant_id=?`).bind(...values)];if(adjustment)statements.push(d.prepare("INSERT INTO inventory_movements (id,tenant_id,branch_id,product_id,user_id,movement_type,quantity,balance_after,reason,reference) VALUES (?,?,?,?,?,?,?,?,?,?)").bind(randomUUID(),T,B,p.id,userId(req),adjustment>0?"adjustment_in":"adjustment_out",adjustment,nextStock,p.reason?.trim()||"Ajuste manual",`AJ-${Date.now()}`));await d.batch(statements);const updated=await d.prepare("SELECT id,sku,barcode,name,category,price,cost,stock,version,active,updated_at updatedAt FROM products WHERE id=?").bind(p.id).first();return Response.json({product:updated});}
