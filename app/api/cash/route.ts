import { randomUUID } from "node:crypto";
import { getRuntimeEnv } from "../../../db/runtime-env";
const T="tenant_demo",B="branch_centro",R="register_2";
const uid=(req:Request)=>`user_${btoa(req.headers.get("oai-authenticated-user-email")||"preview@pos360.local").replace(/[^a-z0-9]/gi,"").slice(0,20)}`;
export async function GET(req:Request){const d=getRuntimeEnv().DB,user=uid(req);await d.prepare("INSERT OR IGNORE INTO cash_registers (id,tenant_id,branch_id,name,active) VALUES (?,?,?,?,1)").bind(R,T,B,"Caja 2").run();const session=await d.prepare("SELECT s.id,s.opening_amount openingAmount,s.status,s.opened_at openedAt,r.name registerName,COALESCE((SELECT SUM(amount) FROM cash_movements WHERE session_id=s.id),0) movements FROM cash_sessions s JOIN cash_registers r ON r.id=s.register_id WHERE s.tenant_id=? AND s.user_id=? AND s.status='open' ORDER BY s.opened_at DESC LIMIT 1").bind(T,user).first();return Response.json({session});}
export async function POST(req:Request){const p=await req.json() as {action?:"open"|"close";amount?:number},d=getRuntimeEnv().DB,user=uid(req);await d.prepare("INSERT OR IGNORE INTO cash_registers (id,tenant_id,branch_id,name,active) VALUES (?,?,?,?,1)").bind(R,T,B,"Caja 2").run();if(p.action==="open"){const current=await d.prepare("SELECT id FROM cash_sessions WHERE tenant_id=? AND user_id=? AND status='open'").bind(T,user).first();if(current)return Response.json({error:"Ya existe una caja abierta"},{status:409});const id=randomUUID(),amount=Number(p.amount||0);await d.batch([d.prepare("INSERT INTO cash_sessions (id,tenant_id,register_id,user_id,opening_amount,status) VALUES (?,?,?,?,?,'open')").bind(id,T,R,user,amount),d.prepare("INSERT INTO cash_movements (id,tenant_id,session_id,user_id,movement_type,amount,reason,reference) VALUES (?,?,?,?,?,?,?,?)").bind(randomUUID(),T,id,user,"opening",amount,"Base inicial",`AP-${Date.now()}`)]);return Response.json({session:{id,openingAmount:amount,status:"open",registerName:"Caja 2"}},{status:201});}if(p.action==="close"){const s=await d.prepare("SELECT id,opening_amount FROM cash_sessions WHERE tenant_id=? AND user_id=? AND status='open'").bind(T,user).first<{id:string;opening_amount:number}>();if(!s)return Response.json({error:"No hay caja abierta"},{status:404});const totals=await d.prepare("SELECT COALESCE(SUM(amount),0) total FROM cash_movements WHERE session_id=?").bind(s.id).first<{total:number}>(),declared=Number(p.amount||0);await d.prepare("UPDATE cash_sessions SET status='closed',closing_amount=?,expected_amount=?,closed_at=CURRENT_TIMESTAMP WHERE id=?").bind(declared,totals?.total||0,s.id).run();return Response.json({closed:true,declared,expected:totals?.total||0,difference:declared-(totals?.total||0)});}return Response.json({error:"Acción inválida"},{status:400});}
